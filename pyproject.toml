[project]
name = "napcatqq-gpt-bot"
version = "0.1.0"
description = "QQ Bot with GPT API integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "websockets>=12.0,<14",
    "requests>=2.31.0",
    "openai>=1.0.0"
]
