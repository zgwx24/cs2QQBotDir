# 群聊白名单功能指南

## 功能说明

取消了原来的 `is_mentioned`（@机器人）机制，改为基于**数字账号的群聊回复白名单**。

只有在白名单中的用户才能收到群聊回复。

## 配置步骤

### 1. 填写管理员账号

在 `qqbot.py` 中找到这一行：

```python
ADMIN_ACCOUNT = 0  # 请填写管理员QQ账号（数字）
```

将 `0` 改为你的 QQ 号，例如：

```python
ADMIN_ACCOUNT = 123456789
```

### 2. 手动添加白名单（可选）

在 `qqbot.py` 中的 `USER_WHITELIST` 配置：

```python
USER_WHITELIST = {
    152103400: [123456789, 987654321],  # 群号: [QQ号列表]
    615988021: [111111111, 222222222],
}
```

- 键是群号（`group_id`）
- 值是该群中允许回复的用户 QQ 号列表

## 动态添加白名单

### 方法

管理员在群里发送消息，格式为：

1. **@mention** 要添加的用户
2. **说出触发词**之一：
   - `攻击他`
   - `给我上`
   - `加入白名单`

### 示例

假设你的 QQ 是管理员，你想让用户 小明 加入白名单：

```
管理员: @小明 攻击他
```

或者：

```
管理员: @小明 给我上
```

Bot 会自动识别这个命令，并将小明的 QQ 号添加到该群的白名单。

### 同时添加多个用户

```
管理员: @小明 @小红 @小刚 攻击他
```

这样会同时添加这三个用户。

## 配置参考

### 群号白名单

控制哪些群启用 Bot 功能：

```python
GROUP_WHITELIST = [
    152103400,  # csqaq 网站交流群
    615988021,  # fbw 9群
]
```

### 触发词自定义

如果要改变触发词，编辑：

```python
ADD_WHITELIST_KEYWORDS = ["攻击他", "给我上", "加入白名单", "你的词"]
```

## 其他模式

### 回复所有消息（暴力模式）

在 `qqbot.py` 中设置：

```python
REPLY_ALL = True
```

这样会忽略白名单，回复所有消息。

### 忽略群号白名单

```python
IGNORE_WHITELIST = True
```

允许所有群使用 Bot。

## 日志输出

启用非静默模式查看详细日志：

```python
QUIET_MODE = False  # 显示所有日志
```

日志会显示：
- ✓ 已添加用户 XXX 到群 YYY 的白名单
- 白名单检查: True/False
- ⏭️  用户不在白名单中，跳过处理

## 常见问题

**Q: 如何移除白名单用户？**

A: 目前需要直接编辑 `qqbot.py` 中的 `USER_WHITELIST` 字典。

**Q: 管理员发送命令后有反馈吗？**

A: 有的，控制台会输出日志信息。如果想在群里看到反馈，可以改进代码添加群回复。

**Q: 白名单数据会持久化吗？**

A: 不会。重启 Bot 后白名单会恢复到代码中配置的初始值。如果需要持久化，可以改进代码保存到文件或数据库。
